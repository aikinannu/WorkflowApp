PORT=8000
HOST=0.0.0.0
NODE_ENV=development

# Database (when ready to integrate MongoDB)
MONGODB_URI=mongodb://localhost:27017/godemars-empire
DB_NAME=godemars-empire

# JWT Configuration
# IMPORTANT: Set a strong secret in your environment manager or Codespaces secrets
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Backend API
API_URL=http://localhost:8000
API_VERSION=v1
